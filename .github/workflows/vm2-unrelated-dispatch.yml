name: VM2 Unrelated Dispatch
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  unrelated-long-run:
    runs-on: ubuntu-latest
    steps:
      - name: Unrelated long-running canary
        run: |
          echo VM2_UNRELATED_DISPATCH_STARTED
          sleep 300
